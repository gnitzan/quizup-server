/**
 * Controller classes which implements the server's API.
 * 
 * Two controllers wraps all the server functionality separated
 * into @See PlayersController which handles authentication and
 * player's related API, and @See GameController which handles
 * Game's related API.
 * 
 * @author rom
 */
package com.rom.quizup.server.controllers;