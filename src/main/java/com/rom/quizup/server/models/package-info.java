/**
 * Contains game resources which are sent back
 * and forth between clients and the server over HTTP.
 * 
 * The resources are serialized to and from JSON by Spring's REST layer.
 * 
 * @author rom
 */
package com.rom.quizup.server.models;